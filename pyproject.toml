[project]
name = "typing-game"
version = "0.1.0"
description = "A typing game built with Python and Tkinter"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = []

[tool.uv]
python-preference = "only-managed"
python-downloads = "automatic"